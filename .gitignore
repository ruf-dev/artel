# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Configs
config/dev.yaml
CLAUDE.local.md

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
# Dependency directories (remove the comment below to include it)
# vendor/
.idea
proto_deps
local_data
proto_modules
node_modules

.verv/local
.verv/notes.local.md

.deploy/
.claude

# Internal implementation/planning notes, not for distribution
docs/tasks/

# graphify knowledge graph is a local dev-tool cache, not shipped with the service
graphify-out/

# Playwright MCP debug logs/screenshots
.playwright-mcp/