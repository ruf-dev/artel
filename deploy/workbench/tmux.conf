# Size the tmux window to the most recently active client instead of the
# smallest/stale attached one — otherwise the ttyd web terminal renders far
# smaller than its container. Requires tmux >= 2.9 (Debian bookworm ships 3.3a).
set -g window-size latest
